The memory context below is a COMPACT SUMMARY of this project's records
(generated {{GENERATED}}), not the full index. The records themselves are
unchanged — every name mentioned below can be read in full on demand with
`lazy_memory_recall(name="<name>")`, and bodies are searchable with
`lazy_search(query="in:memories <text>")`. If a name you need is missing here,
call `lazy_memory_recall()` with no arguments for the complete live index.

{{COMPACT}}
{{RECENT}}{{REMOVED}}
