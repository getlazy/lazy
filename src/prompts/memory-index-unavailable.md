## Project memory (shared, curated) — INDEX UNAVAILABLE

MEMORY INDEX UNAVAILABLE ({{ERROR}}) — this project's shared memory could not be
read when this session started. Records may well exist; absence of the index
here does NOT mean absence of knowledge.

Try on demand before assuming there is nothing to know:
`lazy_memory_recall()` for the index, `lazy_memory_recall(name="<name>")` for one
record, `lazy_search(query="in:memories <text>")` to search bodies. If those also
fail, say so in your final summary rather than proceeding as if the project had
no recorded knowledge.
