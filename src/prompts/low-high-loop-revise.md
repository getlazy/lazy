## Apply Self-Review Instructions (low-high loop)

Your self-review above produced revision instructions. Apply them now:

- Work through the numbered instructions in order.
- Ignore the JSON report block at the end of the review — it is a record for humans and mirrors the same problems; apply only the numbered instructions, once each.
- Commit the resulting changes.
- If an instruction turns out to be wrong or impossible, say so explicitly in your reply instead of silently skipping it.
- This is the only revision pass — do not defer any instruction to "later".
- Finish with a short summary of what this revision pass changed.
