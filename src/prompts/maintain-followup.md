Your work this turn did not touch {{count}} maintained file group(s) that this project expects to be kept up to date:

{{entries}}

For each group, do ONE of the following now:
- If your work affects those files, make the update (and commit it), OR
- If no update is genuinely needed, briefly state why for each group (e.g. "intra-release change, no CHANGELOG entry needed").

Your response is recorded for the human reviewer, so be explicit about which groups you updated and which you deliberately skipped and why. Then hand back control.
