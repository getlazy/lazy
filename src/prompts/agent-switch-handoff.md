# Agent switch — distilled handoff

You are continuing an **existing** lazy task after an **agent switch** (or
another fresh-session start that could not resume the prior agent session).
A previous agent worked on this task in a different product session. That
on-disk session was **not** transplanted. What follows is a **distilled**
reconstruction from lazy's own turn store (and a short branch orientation),
**not** a verbatim transcript of the prior agent's tool calls or private
reasoning.

## What you must do before new work

1. Read the distilled history below (and heed any truncation notice).
2. Inspect the task branch: review the commit list and changed-file summary
   in the orientation section. Prefer those over guessing from memory.
3. **Ignore conflict-free upstream merge commits** called out as sync/auto-merges
   — they are bookkeeping, not task design.
4. Skim the files already touched so you do not redo or fight existing work.
5. Only then continue toward the goal, using the human's latest message as the
   immediate instruction.

Do not claim you remember the prior session. You don't — you have this distillate
plus the git tree.

{{orientation}}

{{turnHistory}}
