STOP. Do NOT call lazy_close again yet.

You are closing task `{{task_code}}` (no work started).

Tell the user you are closing this task, then call `lazy_close` with confirmation_code: "{{confirmation_code}}"