STOP. Do NOT call lazy_reopen again yet.

Task `{{task_code}}` was previously completed and accepted. Reopening may create ambiguity about the already-merged work.

Tell the user this task was already accepted and you are reopening it, then call `lazy_reopen` with confirmation_code: "{{confirmation_code}}"