STOP. Do NOT call lazy_abandon again yet.

You are abandoning task `{{task_code}}` (no work started).

Tell the user you are abandoning this task, then call `lazy_abandon` with confirmation_code: "{{confirmation_code}}"