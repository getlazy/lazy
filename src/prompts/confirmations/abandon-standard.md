STOP. Do NOT call lazy_abandon again yet.

You are abandoning task `{{task_code}}`.

Tell the user you are abandoning this task and why, then call `lazy_abandon` with confirmation_code: "{{confirmation_code}}"